# Required for Drizzle/PostgreSQL repos (not needed for JSON file storage)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/ubc_planner

# Optional — enables BullMQ workers. Leave commented out for JSON-only mode.
# REDIS_URL=redis://localhost:6379

PORT=3000
